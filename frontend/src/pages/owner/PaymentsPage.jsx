import { useState } from 'react';import { useQuery } from '@tanstack/react-query';import { PageHeader } from '../../components/layout/PageHeader';import { Badge } from '../../components/ui/Badge';import { Button } from '../../components/ui/Button';import { PageLoading,ErrorState,EmptyState } from '../../components/feedback/States';import { shopwiseApi } from '../../services/shopwiseApi';import { dateTime,friendly,money } from '../../utils/format';import { Pagination } from './ProductsPage'
export function PaymentsPage(){const[filters,setFilters]=useState({page:1});const query=useQuery({queryKey:['payments',filters],queryFn:()=>shopwiseApi.payments.list(filters)});if(query.isLoading)return <PageLoading/>;if(query.error)return <ErrorState error={query.error}/>;return <><PageHeader title="Payments" description="Paginated payment records loaded in one request."/><form className="mb-4 flex flex-wrap gap-2" onSubmit={e=>{e.preventDefault();setFilters({...Object.fromEntries(new FormData(e.currentTarget)),page:1})}}><input className="control w-auto" name="dateFrom" type="date"/><input className="control w-auto" name="dateTo" type="date"/><select className="control w-auto" name="method"><option value="">All methods</option><option value="cash">Cash</option><option value="mpesa">M-Pesa</option><option value="card">Card</option></select><select className="control w-auto" name="status"><option value="">All statuses</option><option value="pending">Pending</option><option value="completed">Completed</option><option value="failed">Failed</option><option value="refunded">Refunded</option></select><Button variant="secondary">Apply</Button></form><section className="card rounded-xl">{query.data.items.length?<><div className="table-wrap"><table className="data-table"><thead><tr><th>Sale</th><th>Method</th><th>Amount</th><th>Status</th><th>Reference</th><th>Date</th></tr></thead><tbody>{query.data.items.map(p=><tr key={p.id}><td className="font-semibold">{p.receiptNumber}</td><td>{friendly(p.method)}</td><td>{money(p.amount)}</td><td><Badge value={p.status}/></td><td>{p.providerReference||'—'}</td><td>{dateTime(p.createdAt)}</td></tr>)}</tbody></table></div><Pagination data={query.data.pagination} onPage={page=>setFilters({...filters,page})}/></>:<EmptyState title="No payments yet" message="Payments are created automatically during checkout."/>}</section></>}
