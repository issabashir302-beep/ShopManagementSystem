import{useState}from'react';import{Link}from'react-router-dom';import{useQuery}from'@tanstack/react-query';import{shopwiseApi}from'../../services/shopwiseApi';import{PageLoading,ErrorState,EmptyState}from'../../components/feedback/States';import{Badge}from'../../components/ui/Badge';import{dateTime,money}from'../../utils/format';import{Pagination}from'../owner/ProductsPage'
export function PosSalesPage(){const[page,setPage]=useState(1);const query=useQuery({queryKey:['sales','pos',page],queryFn:()=>shopwiseApi.sales.list({page,limit:10})});if(query.isLoading)return <div className="p-6"><PageLoading/></div>;if(query.error)return <div className="p-6"><ErrorState error={query.error}/></div>;return <main className="mx-auto max-w-6xl p-4 sm:p-6"><header className="mb-6"><h1 className="text-2xl font-extrabold">Recent sales</h1><p className="mt-1 text-sm text-gray-500">Persisted sales and payment status for this shop.</p></header><section className="card rounded-xl">{query.data.items.length?<><div className="table-wrap"><table className="data-table"><thead><tr><th>Receipt</th><th>Date</th><th>Total</th><th>Payment</th><th>Status</th></tr></thead><tbody>{query.data.items.map(s=><tr key={s.id}><td><Link to={`/pos/sales/${s.id}`} className="font-bold text-brand-700">{s.receiptNumber}</Link></td><td>{dateTime(s.createdAt)}</td><td>{money(s.totalAmount)}</td><td><Badge value={s.paymentStatus}/></td><td><Badge value={s.status}/></td></tr>)}</tbody></table></div><Pagination data={query.data.pagination} onPage={setPage}/></>:<EmptyState title="No sales yet" message="Checkout activity will appear here."/>}</section></main>}
