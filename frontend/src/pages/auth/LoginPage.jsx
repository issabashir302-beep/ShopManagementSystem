import { useState } from 'react'
import { Link, useLocation, useNavigate } from 'react-router-dom'
import { Button } from '../../components/ui/Button'
import { useAuth } from '../../features/auth/AuthContext'
import { apiMessage } from '../../utils/format'
export function LoginPage() { const auth = useAuth(); const navigate = useNavigate(); const location = useLocation(); const [error, setError] = useState(''); const [loading, setLoading] = useState(false)
  const submit = async (event) => { event.preventDefault(); setError(''); setLoading(true); const data = new FormData(event.currentTarget); try { const identity = await auth.login(data.get('email'), data.get('password')); navigate(location.state?.from?.pathname || (identity.role === 'owner' ? '/app' : '/pos'), { replace: true }) } catch (e) { setError(apiMessage(e)) } finally { setLoading(false) } }
  return <div className="w-full max-w-md"><div className="mb-8 lg:hidden"><span className="grid size-10 place-items-center rounded-lg bg-brand-600 font-bold text-white">S</span></div><p className="text-xs font-bold uppercase tracking-widest text-brand-600">Welcome back</p><h1 className="mt-2 text-3xl font-extrabold">Sign in to Shopwise</h1><p className="mt-2 text-sm text-gray-500">Use your owner or shopkeeper account.</p>{error && <div role="alert" className="mt-5 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</div>}<form onSubmit={submit} className="mt-7 grid gap-5"><label><span className="label">Email address</span><input className="control" name="email" type="email" required autoComplete="email" /></label><label><span className="label">Password</span><input className="control" name="password" type="password" required autoComplete="current-password" /></label><p className="text-xs text-gray-500">Shopkeeper password recovery is requested by the shop owner.</p><Button size="lg" loading={loading}>Sign in</Button></form><p className="mt-6 text-sm text-gray-500">New shop owner? <Link className="font-bold text-brand-600" to="/signup">Create an account</Link></p></div> }
