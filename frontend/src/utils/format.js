export const money = (value, currency = 'KES') => new Intl.NumberFormat('en-KE', { style: 'currency', currency, maximumFractionDigits: 2 }).format(Number(value || 0))
export const dateTime = (value) => value ? new Intl.DateTimeFormat('en-KE', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(value)) : '—'
export const friendly = (value = '') => String(value).replaceAll('_', ' ').replace(/\b\w/g, (letter) => letter.toUpperCase())
export const apiMessage = (error) => ({ UNAUTHENTICATED: 'Your session has expired. Sign in again.', FORBIDDEN: 'You do not have permission to do that.', INSUFFICIENT_STOCK: 'There is not enough stock to complete this sale.', PRODUCT_NOT_FOUND: 'This product is no longer available.', RATE_LIMITED: 'Too many attempts. Please wait and try again.', SHOPKEEPER_ACCOUNT_EXISTS: 'A shopkeeper with this email already exists.' }[error?.code] || error?.message || 'Something went wrong. Please try again.')
